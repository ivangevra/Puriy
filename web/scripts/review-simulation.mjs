import {chromium} from '@playwright/test';
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:960}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.addInitScript(()=>localStorage.setItem('juliaca-signals-v1',JSON.stringify([{id:'signal-qa',name:'Cruce de prueba',approach:'Norte',lat:-15.488,lon:-70.129,status:'unknown',red:4,green:4,amber:2,pedestrian:false,source:'Ejemplo visual',observedAt:'2026-09-06',notes:'',updatedAt:new Date().toISOString()}])));
await page.goto('http://localhost:5173');await page.locator('.transit-map[data-base-ready=true]').waitFor();await page.getByRole('button',{name:'D01',exact:true}).click();await page.getByRole('button',{name:'Simular un micro'}).click();await page.waitForTimeout(1200);await page.screenshot({path:'../artifacts/micro-signal-simulation.png'});
await page.getByRole('button',{name:'Tema oscuro',exact:true}).click();await page.waitForTimeout(1200);await page.screenshot({path:'../artifacts/micro-signal-dark.png'});
await page.getByRole('button',{name:'Viajar',exact:true}).click();
await page.route('https://photon.komoot.io/**',r=>r.fulfill({json:{features:[{geometry:{coordinates:[-70.134,-15.489]},properties:{name:'Origen de prueba',osm_id:99,osm_type:'N',city:'Juliaca'}}]}}));
await page.getByLabel('Desde',{exact:true}).fill('Origen de prueba');await page.getByRole('option',{name:/Origen de prueba/}).click();await page.getByLabel('Hasta',{exact:true}).fill('aeropuerto');await page.getByRole('option',{name:/Aeropuerto Inca Manco Cápac/}).first().click();await page.getByRole('button',{name:'Buscar mi ruta'}).click();await page.locator('.journey-step').first().waitFor();await page.locator('.journey-step').first().scrollIntoViewIfNeeded();await page.screenshot({path:'../artifacts/walking-instructions.png'});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'../artifacts/walking-instructions-mobile.png',fullPage:true});console.log({errors,overflow:await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth)});await browser.close();
