import {chromium} from '@playwright/test';
const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:960}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto('http://localhost:5173');await page.locator('.app-shell[data-ready=true]').waitFor();await page.locator('.transit-map[data-base-ready=true]').waitFor();await page.getByRole('button',{name:'D01',exact:true}).click();
await page.waitForTimeout(500);await page.screenshot({path:'../artifacts/arrows-native-before.png'});
const zoom=page.getByRole('button',{name:'Acercar mapa'});await zoom.click();await page.waitForTimeout(80);await page.screenshot({path:'../artifacts/arrows-native-during.png'});await page.waitForTimeout(500);await page.screenshot({path:'../artifacts/arrows-native-after.png'});
await page.getByRole('button',{name:'Semáforos',exact:true}).click();await page.getByLabel('Cruce o nombre').fill('Cruce de estudio · Circunvalación');await page.getByLabel('Acceso y orientación').fill('Acceso norte · hacia el centro');await page.getByLabel('Latitud del semáforo').fill('-15.49');await page.getByLabel('Longitud del semáforo').fill('-70.13');await page.getByLabel('Rojo',{exact:true}).fill('40');await page.getByLabel('Ámbar',{exact:true}).fill('3');await page.getByLabel('Verde',{exact:true}).fill('27');await page.getByRole('button',{name:'Guardar semáforo',exact:true}).click();
await page.evaluate(()=>{document.querySelector('.editor-panel').scrollTop=0;scrollTo(0,0);});await page.getByRole('button',{name:'Cerrar aviso'}).click();await page.screenshot({path:'../artifacts/signals-desktop.png',fullPage:true});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'../artifacts/signals-mobile.png',fullPage:true});
console.log({errors,overflow:await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth)});await browser.close();
