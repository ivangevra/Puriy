import {chromium} from '@playwright/test';
const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:960}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto('http://localhost:5173');await page.locator('.app-shell[data-ready=true]').waitFor();await page.getByRole('button',{name:'D01',exact:true}).click();await page.locator('.transit-map[data-base-ready=true]').waitFor();await page.screenshot({path:'../artifacts/directions-light.png'});
await page.getByRole('button',{name:'Dibujar',exact:true}).click();await page.getByText('Partir de una ruta del piloto',{exact:true}).click();await page.getByRole('button',{name:'D01 · Corredor norte–sur',exact:true}).click();await page.locator('.transit-map[data-base-ready=true]').waitFor();await page.evaluate(()=>scrollTo(0,0));await page.waitForTimeout(350);await page.screenshot({path:'../artifacts/editor-light.png',fullPage:true});
await page.getByRole('button',{name:'Tema oscuro',exact:true}).click();await page.waitForTimeout(700);await page.locator('.transit-map[data-base-ready=true]').waitFor();await page.screenshot({path:'../artifacts/editor-dark.png',fullPage:true});
await page.setViewportSize({width:390,height:844});await page.screenshot({path:'../artifacts/editor-mobile.png',fullPage:true});console.log({errors,overflow:await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth)});await browser.close();
